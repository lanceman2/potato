// This returns a string that is just a function of the what the input buf
// string is.
extern
char *poRandSequence_string(const char *ibuf,
        char *buf, size_t *rlen,
        size_t minLen, size_t maxLen);
